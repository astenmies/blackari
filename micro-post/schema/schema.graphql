schema {
  query: Query
}

"The query type, represents all of the entry points into our object graph"
type Query {
  getPost(id: ID): Post
}

"what is needed for a post"
type Post {
  id: ID!
  title: String
}
